/* import { AsyncTask } from 'toad-scheduler';

const task = new AsyncTask(
    'task1',
    async () => {
        console.log('Task1 executed');
    },
    async (err) => {
        console.log('Task1 failed', err);
    }
);

const interval = { seconds: 3 };


export default { task, interval };
 */